import { BindBackgroundDirective } from './bind-background.directive';

describe('BindBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new BindBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
