import { ClickToggleDirective } from './click-toggle.directive';

describe('ClickToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickToggleDirective();
    expect(directive).toBeTruthy();
  });
});
