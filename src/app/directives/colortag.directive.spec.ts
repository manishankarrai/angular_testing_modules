import { ColortagDirective } from './colortag.directive';

describe('ColortagDirective', () => {
  it('should create an instance', () => {
    const directive = new ColortagDirective();
    expect(directive).toBeTruthy();
  });
});
